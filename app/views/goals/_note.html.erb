<div id="notes-container">
  <h4>Note to self on <%= note.date %></h4>
  <p>
    <%= note.body %>
  </p>
  <p>Posted <%= distance_of_time_in_words(note.goal.created_at, note.created_at) %> later</p>
</div>
