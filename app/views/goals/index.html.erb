<div id="goals-container">
  <h1>GOALS</h1>

    <% @current_user.goals.each do |goal| %>
    <div class="goals">
      <%= link_to goal.title, goal_path(goal), class: "goal-title" %>
    </div>
    <% end %>
    <div class="new-goal">
    <%= link_to "New Goal", new_goal_path %>
  </div>
</div>
