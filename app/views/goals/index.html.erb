<div id="goals-container">
  <h1>My Goals</h1>

  <ul id="goals">
    <% @current_user.goals.each do |goal| %>
    <li>
      <%= link_to goal.title, goal_path(goal), class: "article-title" %>
    </li>
    <% end %>
  </ul>
</div>

<%= link_to "Create A New Goal", new_goal_path %>
